{
  "name": "foedus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cernere↔Hub 連結契約の横断静的チェッカー (層B / deterministic contract checker)",
  "bin": {
    "foedus": "dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node dist/cli.js contract-check",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
